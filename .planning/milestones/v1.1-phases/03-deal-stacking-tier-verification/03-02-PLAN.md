---
phase: 3
plan: 02
name: premium-feature-wiring
wave: 1
depends_on: []
files_modified:
  - index.html
requirements:
  - DEAL-05
autonomous: true
validation: "03-VALIDATION.md"
---

# Plan 03-02: Premium Feature Wiring & Coming Soon Conversion

## Objective
Replace the three "Coming Soon" chips (Yield Analysis, Comparables, Deal Stacking) with functional gated `<details>/<summary>` sections. Premium users see full content; free-tier users see a blurred preview with an upgrade CTA. This wires up the premium feature gating pattern that Plan 01's deal stacking calculator and Plan 03's tier verification depend on.

## must_haves
- [ ] All three "Coming Soon" chips removed and replaced with `<details>/<summary>` sections
- [ ] Yield Analysis section shows gross yield, net yield, yield rating, and contextual verdict for premium users
- [ ] Comparables section shows street average, guide price, below-market %, and uplift context for premium users
- [ ] Deal Stacking section in left column acts as a teaser/link pointing to the right-column calculator
- [ ] Free-tier users see blurred preview with "Unlock [feature] with Pro" upgrade CTA for all three features
- [ ] Upgrade CTA buttons call `showPaywall()` consistently
- [ ] Premium gating uses existing `isPremium()` function

## Tasks

<task id="03-02-01">
<title>Remove Coming Soon chips and add CSS for premium feature sections</title>
<details>
In `index.html`, locate the "Coming Soon" chip rendering at approximately lines 2751-2754 where `comingSoonTag` is used to create three inline `<div>` elements inside a flex container (Yield Analysis, Comparables, Deal Stacking).

Remove the entire flex container of Coming Soon chips.

Add CSS (in the existing `<style>` block) for the premium feature sections:

```css
.premium-feature {
  margin-top: 12px;
  border: 1px solid rgba(0,0,0,0.08);
  border-radius: 10px;
  overflow: hidden;
}
.premium-feature summary {
  padding: 10px 14px;
  font-family: var(--font-main);
  font-size: 14px;
  font-weight: 600;
  color: var(--text);
  cursor: pointer;
  background: var(--bg-card);
  list-style: none;
  display: flex;
  align-items: center;
  gap: 8px;
}
.premium-feature summary::-webkit-details-marker { display: none; }
.premium-feature summary::after {
  content: '\25B6';
  font-size: 10px;
  margin-left: auto;
  transition: transform 0.2s;
}
.premium-feature[open] summary::after {
  transform: rotate(90deg);
}
.premium-feature .pf-content {
  padding: 12px 14px;
  font-size: 13px;
  color: var(--text);
  border-top: 1px solid rgba(0,0,0,0.06);
}
.premium-feature .pf-blurred {
  position: relative;
  padding: 12px 14px;
  border-top: 1px solid rgba(0,0,0,0.06);
}
.premium-feature .pf-blurred .pf-blur-content {
  filter: blur(4px);
  pointer-events: none;
  user-select: none;
}
.premium-feature .pf-blurred .pf-upgrade-cta {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  text-align: center;
  z-index: 2;
  background: rgba(255,255,255,0.9);
  padding: 16px 24px;
  border-radius: 10px;
  box-shadow: 0 2px 12px rgba(0,0,0,0.1);
}
.premium-feature .pf-upgrade-cta button {
  margin-top: 8px;
  padding: 8px 20px;
  background: var(--accent);
  color: #fff;
  border: none;
  border-radius: 6px;
  font-size: 13px;
  font-weight: 600;
  cursor: pointer;
}
.premium-feature .pf-upgrade-cta button:hover {
  background: var(--accent-hover);
}
```

Use native `<details>/<summary>` elements per CLAUDE.md guidance (no JS-driven accordions).
</details>
<verify>
Verify the Coming Soon chips are no longer rendered in the expanded panel. Verify the new CSS is present in the style block. Check there are no remaining references to `comingSoonTag` in the expanded panel HTML construction.
</verify>
</task>

<task id="03-02-02">
<title>Build Yield Analysis premium feature section</title>
<details>
In the expanded panel left column HTML construction (around line 2722 in index.html), where the Coming Soon chips were, add the Yield Analysis `<details>/<summary>` section.

**Premium content (when `isPremium()` is true):**
```html
<details class="premium-feature">
  <summary>Yield Analysis</summary>
  <div class="pf-content">
    <!-- Yield data -->
  </div>
</details>
```

Content to render using lot data fields:
- **Gross Yield:** `lot.estGrossYield` (display as X.X% or "N/A" if missing)
- **Net Yield:** Calculate as `grossYield * 0.867` (after 10% management + 4-week void). Display as X.X%
- **Monthly Rent:** `lot.estMonthlyRent` formatted as £X,XXX or "N/A"
- **Yield Rating:** Based on gross yield:
  - Good (>= 7%): green badge
  - Fair (5-7%): amber badge
  - Poor (< 5%): red badge
  - N/A: grey badge (when yield data missing)
- **Contextual verdict:** Generate a one-line text:
  - Good: "Strong rental yield — above average for this area"
  - Fair: "Moderate yield — typical for the area"
  - Poor: "Below-average yield — consider capital growth potential instead"

**Free-tier content (when `!isPremium()`):**
```html
<details class="premium-feature">
  <summary>Yield Analysis <span style="font-size:11px; color:var(--accent-warn);">PRO</span></summary>
  <div class="pf-blurred">
    <div class="pf-blur-content">
      <!-- Dummy/structure content showing what premium looks like -->
      <div style="display:flex; justify-content:space-between; margin-bottom:8px;">
        <span>Gross Yield</span><span>X.X%</span>
      </div>
      <div style="display:flex; justify-content:space-between; margin-bottom:8px;">
        <span>Net Yield</span><span>X.X%</span>
      </div>
      <div style="display:flex; justify-content:space-between;">
        <span>Rating</span><span style="background:#e8f5e9; padding:2px 8px; border-radius:4px;">Good</span>
      </div>
    </div>
    <div class="pf-upgrade-cta">
      <div style="font-size:14px; font-weight:600; color:var(--text);">Unlock Yield Analysis with Pro</div>
      <div style="font-size:12px; color:var(--text-muted); margin-top:4px;">From &pound;9.99/mo</div>
      <button onclick="showPaywall('yield_analysis')">Upgrade Now</button>
    </div>
  </div>
</details>
```
</details>
<verify>
1. Set `window._userTier = 'premium'` — expand Yield Analysis, verify gross/net yield, rating badge, and verdict text display correctly
2. Set `window._userTier = 'free'` — expand Yield Analysis, verify blurred preview with upgrade CTA
3. Click "Upgrade Now" — verify `showPaywall()` modal appears
4. Test with a lot that has no yield data — verify "N/A" handling
</verify>
</task>

<task id="03-02-03">
<title>Build Comparables premium feature section</title>
<details>
Below the Yield Analysis section in the left column, add the Comparables `<details>/<summary>` section.

**Premium content:**
- **Street Average Price:** `lot.streetAvg` formatted as £X,XXX or "No comparable data available"
- **Guide Price:** `lot.price` formatted as £X,XXX
- **Below Market %:** `lot.belowMarket` — display as "X% below market average" with green text if positive, or "X% above market average" with red text if negative, or "At market level" if 0/null
- **Contextual note:**
  - If belowMarket > 20%: "Significant discount — strong uplift potential on refurb"
  - If belowMarket 10-20%: "Moderate discount — potential for value-add"
  - If belowMarket 0-10%: "Marginal discount — limited upside without development"
  - If belowMarket <= 0 or null: "At or above market — ensure your exit strategy supports the numbers"

**Free-tier content:** Same blurred preview pattern as Yield Analysis with dummy structure content and upgrade CTA: "Unlock Comparables with Pro" + `showPaywall('comparables')`.

Use the same `<details class="premium-feature">` pattern as task 03-02-02.
</details>
<verify>
1. Premium user: expand Comparables, verify street average, guide price, below-market %, and contextual note
2. Free user: verify blurred preview + upgrade CTA
3. Lot with no streetAvg data: verify "No comparable data available" message
4. Lot with belowMarket > 20%: verify "Significant discount" note
</verify>
</task>

<task id="03-02-04">
<title>Build Deal Stacking teaser section in left column</title>
<details>
Below the Comparables section in the left column, add a Deal Stacking `<details>/<summary>` teaser.

This is NOT the full calculator (that lives in the right column from Plan 01). This is a teaser/link in the left column among the other premium features.

**Premium content:**
```html
<details class="premium-feature">
  <summary>Deal Stacking Calculator</summary>
  <div class="pf-content" style="text-align:center; padding:16px;">
    <p style="color:var(--text-muted); font-size:13px; margin:0 0 8px;">
      Full deal stacking calculator is in the Finance section to the right.
    </p>
    <p style="color:var(--text-muted); font-size:12px; margin:0;">
      Enter GDV and works cost to see Flip vs Hold analysis.
    </p>
  </div>
</details>
```

**Free-tier content:** Blurred preview showing a mock deal stack summary (fake numbers), with upgrade CTA: "Unlock Deal Stacking with Pro" + `showPaywall('deal_stacking')`.

The blurred preview should show enough structure to create FOMO — a mini version of what the calculator looks like:
```html
<div class="pf-blur-content">
  <div style="display:grid; grid-template-columns:1fr 1fr; gap:8px; font-size:13px;">
    <div><strong>Flip</strong></div><div><strong>Hold</strong></div>
    <div>Net Profit: £XX,XXX</div><div>Cashflow/mo: £XXX</div>
    <div>ROI: XX.X%</div><div>CoC: XX.X%</div>
  </div>
</div>
```
</details>
<verify>
1. Premium user: expand Deal Stacking teaser, verify it points to the right column calculator
2. Free user: verify blurred preview with mock numbers + upgrade CTA
3. Verify the upgrade CTA calls `showPaywall('deal_stacking')`
</verify>
</task>

<task id="03-02-05">
<title>Gate the deal stacking widget in right column for free users</title>
<details>
In the expanded panel right column where the deal stacking widget is rendered (from Plan 01, task 03-01-02), ensure that free-tier users see an upgrade prompt instead of the full calculator.

Modify the widget rendering logic:

If `!isPremium()`, instead of the full calculator HTML, render:
```html
<div class="deal-stack-widget" style="margin-top:16px; padding:20px; background:var(--bg-card); border-radius:12px; border:1px solid rgba(0,0,0,0.08); text-align:center;">
  <h4 style="margin:0 0 8px; font-family:var(--font-main); font-size:15px; font-weight:600; color:var(--text);">
    Deal Stacking Calculator
  </h4>
  <p style="color:var(--text-muted); font-size:13px; margin:0 0 12px;">
    Analyse Flip vs Hold scenarios with live lender data
  </p>
  <button onclick="showPaywall('deal_stacking')"
    style="padding:10px 24px; background:var(--accent); color:#fff; border:none; border-radius:8px; font-size:14px; font-weight:600; cursor:pointer;">
    Upgrade to Pro — &pound;9.99/mo
  </button>
</div>
```

This ensures DEAL-05 is fully satisfied: deal stacking is premium-only, free users see upgrade prompt, no "Coming Soon" chip remains.
</details>
<verify>
1. Set `window._userTier = 'free'` — open expanded panel, verify upgrade prompt appears in right column where calculator would be
2. Click "Upgrade to Pro" — verify `showPaywall()` fires
3. Set `window._userTier = 'premium'` — open expanded panel, verify full calculator appears
4. Search entire codebase for "Coming Soon" — verify no references remain in the lot expansion code
</verify>
</task>

## Verification
- No "Coming Soon" chips remain in the expanded panel
- Three `<details>/<summary>` premium feature sections render in the left column
- Each section shows correct content for premium users and blurred preview for free users
- All upgrade CTAs trigger `showPaywall()` with appropriate reason
- Deal stacking widget in right column is gated (premium = calculator, free = upgrade prompt)
- DEAL-05 is fully satisfied
