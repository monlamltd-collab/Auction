---
phase: 04-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [server.js, tests/test-gating.js]
autonomous: true
requirements: [GATE-01, GATE-02, GATE-05]

must_haves:
  truths:
    - "Stripe endpoints return 503 when STRIPE_ENABLED=false"
    - "Signed-in users resolve to 'premium' tier when Stripe is disabled"
    - "Anonymous users resolve to 'anon' tier"
    - "Signed-in users get 50 AI searches/day rate limit when Stripe disabled"
    - "All existing functionality unchanged when STRIPE_ENABLED is not set (backwards compatible)"
  artifacts:
    - path: "tests/test-gating.js"
      provides: "Automated tests for feature flag, tier resolution, and rate limits"
      min_lines: 40
    - path: "server.js"
      provides: "STRIPE_ENABLED flag, resolveEffectiveTier(), updated rate limits"
      contains: "STRIPE_ENABLED"
  key_links:
    - from: "server.js"
      to: "resolveEffectiveTier"
      via: "function call replacing inline tier checks"
      pattern: "resolveEffectiveTier\\(user\\)"
    - from: "server.js"
      to: "STRIPE_ENABLED"
      via: "env var read at top of file"
      pattern: "const STRIPE_ENABLED"
---

<objective>
Implement server-side Stripe hibernation: feature flag, centralised tier resolution, and updated rate limits.

Purpose: This is the backend foundation for the free-first pivot. All Stripe endpoints get guarded behind STRIPE_ENABLED, tier resolution becomes a single function (not scattered inline checks), and rate limits adjust to the new 50/day model for signed-in users.
Output: server.js with STRIPE_ENABLED flag, resolveEffectiveTier(), updated getAISearchLimit(), and automated tests.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-foundation/04-RESEARCH.md

<interfaces>
<!-- Key patterns from research - exact line numbers and code structures -->

Stripe endpoint locations in server.js (from research):
- /api/stripe/checkout (line ~1301)
- /api/stripe/webhook (line ~1350) - keep alive for cancellation confirmations
- /api/stripe/portal (line ~1540)
- /api/stripe/status (line ~1560)
- /api/stripe/diag (line ~1289)

Current tier check patterns (fragmented, to be replaced):
- server.js:2803 — `const userTier = user.tier || 'free';`
- server.js:2850 — `const isPremiumOrTrial = userTier === 'premium' || userTier === 'trial';`
- server.js:3244 — same pattern repeated
- server.js:1849-1855 — `getAISearchLimit(user)` checks tier and trial_expires_at

Current rate limit:
- `RATE_LIMIT = 5` (for /api/analyse)
- `getAISearchLimit(user)` returns different limits per tier

Stripe client init (to be guarded):
- `const stripe = new Stripe(process.env.STRIPE_SECRET_KEY)`

CSP header (line ~91-92):
- Contains `checkout.stripe.com` in connect-src and frame-src

New user auto-creation (line ~889):
- `validateUserFromReq()` auto-creates with `tier: 'premium'` and 14-day trial
- When !STRIPE_ENABLED, should create with `tier: 'free'` instead
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create gating test scaffold (Wave 0) and implement STRIPE_ENABLED flag + resolveEffectiveTier</name>
  <files>tests/test-gating.js, server.js</files>
  <behavior>
    - resolveEffectiveTier(null) returns 'anon'
    - resolveEffectiveTier(user) returns 'premium' when STRIPE_ENABLED=false
    - resolveEffectiveTier(user with tier='premium') returns 'premium' when STRIPE_ENABLED=true
    - resolveEffectiveTier(user with valid trial) returns 'premium' when STRIPE_ENABLED=true
    - resolveEffectiveTier(user with tier='free', no trial) returns 'free' when STRIPE_ENABLED=true
    - getAISearchLimit(null) returns anon limit (existing value)
    - getAISearchLimit(user) returns 50 when STRIPE_ENABLED=false
    - getAISearchLimit(premium user) returns Infinity when STRIPE_ENABLED=true
  </behavior>
  <action>
    RED phase: Create `tests/test-gating.js` using the project's existing test pattern (vanilla assertions, no test library). Import or inline the `resolveEffectiveTier` and `getAISearchLimit` functions. Write tests for all behaviors listed above. Run tests — they must fail (functions don't exist yet).

    GREEN phase: In server.js:
    1. Add `const STRIPE_ENABLED = process.env.STRIPE_ENABLED !== 'false';` near top, after env var reads
    2. Guard Stripe client init: `const stripe = STRIPE_ENABLED && process.env.STRIPE_SECRET_KEY ? new Stripe(process.env.STRIPE_SECRET_KEY) : null;`
    3. Create `resolveEffectiveTier(user)` function per research Pattern 2
    4. Add `const SIGNED_IN_DAILY_LIMIT = 50;` constant
    5. Update `getAISearchLimit(user)` to return SIGNED_IN_DAILY_LIMIT when !STRIPE_ENABLED
    6. Replace all inline tier checks (at lines ~2803, ~2850, ~3244) with `resolveEffectiveTier(user)` calls
    7. In `validateUserFromReq()` (line ~889): when !STRIPE_ENABLED, set new user tier to 'free' instead of 'premium'
    8. Run tests — they must pass

    IMPORTANT: The test file must be runnable standalone. Since server.js is a monolith, extract the testable functions into the test file or use a pattern like `eval()` on extracted function text. Follow whatever pattern test-extractors.js uses for importing from the monolith.

    Note: STRIPE_ENABLED defaults to true (backwards compatible) — only disabled when explicitly set to 'false'.
  </action>
  <verify>
    <automated>node tests/test-gating.js</automated>
  </verify>
  <done>resolveEffectiveTier and getAISearchLimit work correctly for all tier/flag combinations. Inline tier checks replaced. Tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Guard Stripe endpoints and update CSP</name>
  <files>server.js</files>
  <action>
    1. Add guard to each Stripe endpoint (checkout, portal, status, diag):
       ```javascript
       if (!STRIPE_ENABLED) return res.status(503).json({ error: 'payments_hibernated' });
       ```
    2. For `/api/stripe/webhook`: keep it alive but ONLY process `customer.subscription.deleted` events. Add early return for all other event types when !STRIPE_ENABLED.
    3. For `/api/stripe/status`: when !STRIPE_ENABLED, return `{ active: false, stripeEnabled: false }` (not 503, since clients may check this)
    4. Update CSP header (line ~91-92): when !STRIPE_ENABLED, remove `checkout.stripe.com` from connect-src and frame-src
    5. Add `stripeEnabled: STRIPE_ENABLED` to the `/api/auth/me` response object so the client can read it
    6. Update both rate limit paths to use 50/day when !STRIPE_ENABLED:
       - The `RATE_LIMIT` constant used by `/api/analyse` — change to: `const RATE_LIMIT = STRIPE_ENABLED ? 5 : SIGNED_IN_DAILY_LIMIT;`
       - `getAISearchLimit()` already updated in Task 1
  </action>
  <verify>
    <automated>node tests/test-gating.js && npm test</automated>
  </verify>
  <done>All Stripe endpoints guarded. Webhook only processes subscription deletions when Stripe disabled. CSP cleaned. stripeEnabled exposed in /api/auth/me. Both rate limit paths updated.</done>
</task>

</tasks>

<verification>
1. `node tests/test-gating.js` — all tier and rate limit tests pass
2. `npm test` — existing extractor tests still pass (no regressions)
3. Grep `server.js` for `resolveEffectiveTier` — should appear at all former inline tier check locations
4. Grep `server.js` for `STRIPE_ENABLED` — should appear at flag definition, Stripe init, all endpoint guards, CSP, auth/me response
</verification>

<success_criteria>
- STRIPE_ENABLED flag controls all Stripe functionality
- resolveEffectiveTier centralises tier logic (no inline checks remain)
- Rate limits: 50/day for signed-in when Stripe disabled, existing limits preserved when enabled
- Webhook stays alive for subscription.deleted only
- stripeEnabled exposed to client via /api/auth/me
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/04-foundation/04-02-SUMMARY.md`
</output>
