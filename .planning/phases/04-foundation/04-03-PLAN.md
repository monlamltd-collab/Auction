---
phase: 04-foundation
plan: 03
type: execute
wave: 2
depends_on: [04-02]
files_modified: [index.html]
autonomous: false
requirements: [GATE-03, GATE-04, FIX-08]

must_haves:
  truths:
    - "No 'Upgrade', 'Go Pro', or pricing text visible anywhere when Stripe is disabled"
    - "Anonymous users see 'Sign in free' prompt instead of 'Upgrade' on blurred lots"
    - "Paywall modal shows sign-up form instead of pricing cards when Stripe disabled"
    - "CSV export is blocked for anonymous users"
    - "All payment-oriented text replaced with sign-in-oriented text"
  artifacts:
    - path: "index.html"
      provides: "Client-side gating pivot and CSV tier check"
      contains: "stripeEnabled"
  key_links:
    - from: "index.html"
      to: "/api/auth/me"
      via: "reads stripeEnabled from response"
      pattern: "stripeEnabled"
    - from: "index.html"
      to: "showPaywall"
      via: "redirects to signup modal when Stripe disabled"
      pattern: "signupModal"
---

<objective>
Pivot the frontend from paid-tier to free-first: replace all payment UI with sign-in prompts, hide Stripe-specific elements, and add server-side CSV export guard.

Purpose: With the server returning stripeEnabled=false (from Plan 02), the client must hide all payment flows and show "Sign in free" messaging instead. This completes the user-facing free-first pivot.
Output: index.html with all payment UI hidden/replaced when Stripe disabled, CSV export guarded.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-foundation/04-RESEARCH.md

<interfaces>
<!-- From Plan 02: server now returns stripeEnabled in /api/auth/me response -->
<!-- Client reads: window._stripeEnabled = authResponse.stripeEnabled -->

Key client-side locations (from research):
- index.html:1072 — `function isPremium(){return window._userTier==='premium';}`
- index.html:723-758 — signupModal
- showPaywall() function — currently shows pricing cards
- Blurred card overlay — text says "Upgrade for full address..."
- setQ() function — currently calls showPaywall for non-premium users

Text patterns to find and update (grep for all):
- "Upgrade" (10+ locations)
- "Pro" (in context of tier/payment)
- "payment", "checkout", "subscribe", "pricing", "paywall", "Go Pro"

CSV export:
- index.html:2815 — dlCSV() checks window._userTier client-side only
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read stripeEnabled flag and pivot all payment UI to sign-in prompts (GATE-03, GATE-04)</name>
  <files>index.html</files>
  <action>
    1. In the auth/me response handler, store `window._stripeEnabled = data.stripeEnabled;`

    2. Update `showPaywall(reason)`:
       - When `!window._stripeEnabled`: show `signupModal` instead (classList.add('show')), then return early
       - Keep existing paywall code intact (wrapped in else block) for future Stripe reactivation

    3. Update `isPremium()`:
       - When `!window._stripeEnabled`: return `!!window._userTier` (signed-in = premium)
       - When Stripe enabled: keep existing logic

    4. Grep for ALL instances of these strings and update each:
       - "Upgrade for full address" → "Sign in free for full details" (blurred card overlay)
       - "Upgrade" in button text/CTA → "Sign in free" (when in payment context)
       - "Go Pro" → hide or replace with "Sign in"
       - Pricing cards in paywall modal → hide when !stripeEnabled (use CSS display:none or JS conditional)
       - Any "subscribe" text → hide when !stripeEnabled

    5. Ensure `setQ()` shows signupModal (not paywall) when user not signed in and Stripe disabled

    6. Hide any Stripe-specific UI elements (checkout buttons, portal links, subscription management) when !stripeEnabled. Use `style.display = 'none'` or conditional rendering.

    IMPORTANT: Do NOT delete any payment code. Wrap in conditionals so Stripe can be re-enabled later. The paywall modal HTML stays in the DOM but is never shown when Stripe is disabled.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>All payment-oriented text replaced with sign-in text when Stripe disabled. No "Upgrade", "Go Pro", or pricing visible. Paywall redirects to signup modal. Existing payment code preserved for reactivation.</done>
</task>

<task type="auto">
  <name>Task 2: Guard CSV export for anonymous users (FIX-08)</name>
  <files>index.html</files>
  <action>
    Update `dlCSV()` function (line ~2815):
    1. Change the guard from `window._userTier !== 'premium'` to `!window._userTier` (must be signed in)
    2. When blocked, show the signup modal instead of paywall: `$('signupModal').classList.add('show'); return;`
    3. This is sufficient because: (a) all signed-in users are now "premium" when Stripe disabled, (b) the server already strips sensitive fields from /api/all-lots for anonymous users so there's nothing exploitable to export

    Note: A server-side `/api/export-check` endpoint is unnecessary per research analysis — the client-side check combined with server-side data stripping is adequate.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>CSV export requires sign-in. Anonymous users see signup prompt instead of export. No bypass possible since server strips data for anon users.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify free-first gating pivot visually</name>
  <files>index.html</files>
  <action>Human verification of the complete client-side gating pivot. All payment UI should be hidden when Stripe is disabled, sign-in prompts should appear instead, and CSV export should be guarded for anonymous users.</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Human confirms: no payment text visible, sign-in prompts work, CSV export guarded, all features accessible when signed in.</done>
  <what-built>Complete client-side gating pivot: all payment UI hidden, sign-in prompts shown instead, CSV export guarded</what-built>
  <how-to-verify>
    1. Start the server locally with `STRIPE_ENABLED=false node server.js`
    2. Open the app in a browser (not signed in):
       - Verify blurred lot cards show "Sign in free for full details" (not "Upgrade")
       - Click on a blurred card — should show sign-up modal (not pricing/paywall)
       - Try to export CSV — should show sign-up modal
       - Search the page source (Ctrl+U) for "Upgrade", "Go Pro", "checkout" — none should be visible in rendered UI
    3. Sign in with a test account:
       - Verify all AI features are accessible (smart search, analyser, scores, deal stacking)
       - Verify CSV export works
       - Verify no payment prompts appear anywhere
    4. Check at 375px mobile width — sign-up modal should be usable
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
1. `npm test` — existing tests pass
2. Visual: no "Upgrade"/"Go Pro"/"pricing" text visible when STRIPE_ENABLED=false
3. Visual: sign-up modal appears for all gated actions when not signed in
4. Visual: all features accessible when signed in
</verification>

<success_criteria>
- Zero payment-related text visible when Stripe disabled
- Anonymous users prompted to sign in (not upgrade)
- Signed-in users have full access to all features
- CSV export blocked for anonymous, works for signed-in
- All payment code preserved (not deleted) for future reactivation
</success_criteria>

<output>
After completion, create `.planning/phases/04-foundation/04-03-SUMMARY.md`
</output>
