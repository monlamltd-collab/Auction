---
phase: 04-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [INFR-01, INFR-02, INFR-03]

must_haves:
  truths:
    - "Supabase plan tier is confirmed (free or paid) and documented"
    - "All active Stripe subscriptions are cancelled (or confirmed none exist)"
    - "Railway memory/CPU usage is recorded as a baseline before free-tier traffic increase"
  artifacts: []
  key_links: []
---

<objective>
Verify infrastructure readiness before any code changes to Stripe or gating logic.

Purpose: CRIT-1 and CRIT-4 blockers from STATE.md require human verification of external dashboards (Supabase plan, Stripe subscribers, Railway capacity) before hibernating Stripe. Doing this first prevents orphaned billing and scaling surprises.
Output: Confirmed infrastructure state documented in checkpoint, unblocking Plans 02-04.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-foundation/04-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Verify Supabase plan tier (INFR-01)</name>
  <action>
    Check the Supabase dashboard to confirm whether the project is on the free tier or a paid plan.
    - Go to: Supabase Dashboard -> Project Settings -> Billing
    - Record the plan name (Free / Pro / Team)
    - Free tier limits: 500MB DB, 2GB bandwidth, 50K monthly active users
    - If on free tier, evaluate whether this is sufficient for expected free-first traffic
  </action>
  <resume-signal>Report: "Supabase is on [plan name] tier" or "Supabase needs upgrade to [plan]"</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Cancel active Stripe subscriptions and verify (INFR-02)</name>
  <action>
    Check the Stripe dashboard for any active subscriptions before hibernating payment code.
    1. Go to: Stripe Dashboard -> Customers -> filter by "Active subscriptions"
    2. Record the count of active subscribers
    3. If count > 0: Cancel each subscription (immediate, not end-of-period) and note customer emails
    4. If count = 0: Confirm no active subscriptions exist
    5. After cancellation, run this SQL in Supabase SQL Editor:
       ```sql
       UPDATE users SET tier = 'free', stripe_subscription_id = NULL, tier_expires_at = NULL
       WHERE tier = 'premium' AND stripe_subscription_id IS NOT NULL;
       ```
    6. Verify the UPDATE affected the expected number of rows
  </action>
  <resume-signal>Report: "[N] subscribers cancelled and DB updated" or "0 active subscribers, no action needed"</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Verify Railway memory/CPU baseline (INFR-03)</name>
  <action>
    Check Railway metrics dashboard to establish a baseline before increasing traffic.
    1. Go to: Railway Dashboard -> Project -> Metrics
    2. Record current memory usage (MB) and CPU utilization (%)
    3. Note the instance type and any resource limits configured
    4. Check if there have been any OOM kills or restarts in the last 7 days
  </action>
  <resume-signal>Report: "Railway baseline: [X]MB memory, [Y]% CPU, [instance type]. OOM kills: [yes/no]"</resume-signal>
</task>

</tasks>

<verification>
All three infrastructure items verified by human. Results documented in checkpoint responses. No code changes in this plan.
</verification>

<success_criteria>
- Supabase plan tier is known and documented
- Stripe subscriber count is zero (cancelled if needed, DB cleaned)
- Railway baseline metrics recorded
- No blockers remain for Stripe hibernation code changes
</success_criteria>

<output>
After completion, create `.planning/phases/04-foundation/04-01-SUMMARY.md`
</output>
