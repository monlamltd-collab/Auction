---
phase: 04-foundation
plan: 04
type: execute
wave: 3
depends_on: [04-03]
files_modified: [index.html]
autonomous: false
requirements: [FIX-01, FIX-02, FIX-03, FIX-04, FIX-05, FIX-06, FIX-07]

must_haves:
  truths:
    - "Heavy refurb button triggers search execution, not just input population"
    - "Score sort orders lots by score descending within each tier"
    - "Filtering to zero results shows a helpful empty state message"
    - "Search input is trimmed of whitespace and debounced (300ms)"
    - "Navigating to page 0 or negative page is impossible"
    - "Deal stacking widget displays as single column on mobile (375px)"
    - "Sign-in modal text fits within container at 320px viewport width"
  artifacts:
    - path: "index.html"
      provides: "All 7 bug fixes in frontend"
      contains: "runSmartSearch"
  key_links:
    - from: "index.html setQ()"
      to: "runSmartSearch()"
      via: "function call after setting input value"
      pattern: "runSmartSearch\\(\\)"
    - from: "index.html goPage()"
      to: "Math.max"
      via: "page number clamping"
      pattern: "Math\\.max\\(1"
---

<objective>
Fix all 7 remaining known bugs in the frontend: search execution, sort ordering, empty states, input handling, pagination guard, mobile reflow, and text overflow.

Purpose: These bugs degrade user experience and must be fixed before the free-first launch drives more traffic. Each fix is isolated and small, but collectively they make the platform feel polished.
Output: index.html with all 7 bugs resolved.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-foundation/04-RESEARCH.md

<interfaces>
<!-- Bug locations from research -->

FIX-01: index.html:1308-1311 — setQ() function
  Current: sets input value only, never calls search
  Fix: add runSmartSearch() call

FIX-02: index.html:2451-2458 — renderLots sorting
  Current: scoreThenPrice comparator may not sort within tiers
  Fix: verify (b.score - a.score) || (a.price - b.price) within each tier group

FIX-03: index.html renderLots() — after filtering
  Current: no zero-results check
  Fix: add empty state div when lots.length === 0

FIX-04: index.html:1295 — search input handling
  Current: trims but no debounce
  Fix: wrap in debounce (300ms), pattern exists: debounceDealStack()

FIX-05: index.html:1136 — goPage() function
  Current: no bounds check
  Fix: Math.max(1, Math.min(p, totalPages || 1))

FIX-06: index.html:3141-3175 — deal-stack-widget
  Current: inline flexbox, no responsive breakpoint
  Fix: @media (max-width: 600px) single column

FIX-07: index.html:723-758 — signupModal
  Current: max-width:420px, may overflow
  Fix: box-sizing: border-box; overflow-wrap: break-word; responsive padding
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix search, sort, empty state, input handling, and pagination (FIX-01 through FIX-05)</name>
  <files>index.html</files>
  <action>
    FIX-01 — Heavy refurb triggers search:
    - In `setQ(q)` (line ~1308): after `$('smartQuery').value = q;`, add `runSmartSearch();`
    - Also update the guard: when user not signed in, show signupModal (not paywall) since Stripe is disabled. Use: `if(!window._userTier){ $('signupModal').classList.add('show'); return; }`

    FIX-02 — Score sort within tiers:
    - In `renderLots()` sorting section (line ~2451): verify the `scoreThenPrice` comparator sorts `(b.score - a.score) || (a.price - b.price)`. If the tier grouping re-orders lots after sorting, ensure the within-tier order is preserved. The key issue is that lots may be grouped into tiers AFTER sorting, which could lose the sort order. Fix: sort within each tier group independently, or ensure the grouping preserves sort order.

    FIX-03 — Empty state messaging:
    - In `renderLots()`, after all filtering is applied and before rendering cards: if `lots.length === 0`, set the results grid innerHTML to a helpful message:
      ```html
      <div style="text-align:center;padding:40px 20px;color:var(--text-muted)">
        <div style="font-size:1.5rem;margin-bottom:12px">No lots found</div>
        <p>Try adjusting your filters or search terms.</p>
      </div>
      ```
    - Also handle the case where smart search returns 0 results: show "No results for '[query]'. Try different search terms."

    FIX-04 — Search input debounce:
    - Find the search input change/keyup handler. Wrap the search trigger in a debounce using the same pattern as `debounceDealStack()`:
      ```javascript
      let _searchDebounce;
      function debouncedSearch() {
        clearTimeout(_searchDebounce);
        _searchDebounce = setTimeout(() => { runSmartSearch(); }, 300);
      }
      ```
    - Ensure the search input value is trimmed before executing search (research says trim already happens, but verify).

    FIX-05 — Negative page guard:
    - In `goPage(p)` (line ~1136): change to `_currentPage = Math.max(1, Math.min(p, totalPages || 1));`
    - Also check the pagination rendering in `renderLots()` (line ~2469) — if `_currentPage` is clamped there too, ensure consistency.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Heavy refurb executes search. Score sort orders within tiers. Empty state shows helpful message. Search is debounced at 300ms and trimmed. Negative/zero pages impossible.</done>
</task>

<task type="auto">
  <name>Task 2: Fix mobile reflow and sign-in text overflow (FIX-06, FIX-07)</name>
  <files>index.html</files>
  <action>
    FIX-06 — Deal stacking mobile reflow:
    - Find the deal-stack-widget styles (line ~3141-3175). Add a responsive CSS rule in the existing style block:
      ```css
      @media (max-width: 600px) {
        .deal-stack-widget {
          flex-direction: column !important;
        }
        .deal-stack-widget > * {
          width: 100% !important;
          min-width: 0 !important;
        }
      }
      ```
    - Inspect the actual widget structure first — the class name and children may differ from research. Adjust selectors accordingly.
    - Test: elements should stack vertically at 375px width.

    FIX-07 — Sign-in page text overflow:
    - Find the signupModal styles (line ~723-758). Add:
      ```css
      #signupModal .modal,
      #signupModal [class*="modal"] {
        box-sizing: border-box;
        overflow-wrap: break-word;
        word-wrap: break-word;
        max-width: 100vw;
        padding: 20px;
      }
      @media (max-width: 440px) {
        #signupModal .modal,
        #signupModal [class*="modal"] {
          max-width: calc(100vw - 32px);
          margin: 16px;
        }
      }
      ```
    - Check: at 320px viewport, no text should overflow the container. The features list inside the modal is the most likely culprit — ensure it wraps properly.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Deal stacking widget shows single column at 375px. Sign-in modal text fits within container at 320px. No horizontal scrollbar on mobile.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify all 7 bug fixes visually</name>
  <files>index.html</files>
  <action>Human verification of all 7 frontend bug fixes: search execution, sort ordering, empty states, debounce, pagination guard, mobile reflow, and text overflow.</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Human confirms all 7 bugs are fixed: heavy refurb executes search, score sorts within tiers, empty states show messages, search is debounced, negative pages guarded, deal stacking reflows on mobile, sign-in text fits container.</done>
  <what-built>All 7 frontend bug fixes: search execution, sort ordering, empty states, debounce, pagination guard, mobile reflow, text overflow</what-built>
  <how-to-verify>
    1. Start server: `STRIPE_ENABLED=false node server.js`
    2. Sign in with a test account, then:

    FIX-01: Click any "Heavy refurb" quick filter button — search should execute immediately (not just populate the input)

    FIX-02: Sort by "Score" — within each tier (e.g., all 8+ score lots), verify lots are ordered highest score first

    FIX-03: Apply filters that return 0 results (e.g., max price = 1) — should see "No lots found. Try adjusting your filters." message instead of blank space

    FIX-04: Type quickly in the search box — network tab should show only ONE request fires after you stop typing (debounce working). Type "  test  " with leading/trailing spaces — search should use "test" (trimmed)

    FIX-05: Try to navigate to page 0 or negative page via browser console: `goPage(-1)` — page should stay at 1. `goPage(0)` — page should stay at 1.

    FIX-06: Resize browser to 375px width. Open deal stacking on a lot — widget should display as single column (not overflowing horizontally)

    FIX-07: Resize browser to 320px width. Sign out, click any gated feature to trigger sign-in modal — all text should fit within the modal container (no overflow, no horizontal scroll)
  </how-to-verify>
  <resume-signal>Type "approved" or list which fixes need adjustment</resume-signal>
</task>

</tasks>

<verification>
1. `npm test` — existing tests pass (no regressions)
2. All 7 bugs verified visually per checkpoint instructions
</verification>

<success_criteria>
- Heavy refurb button executes search (FIX-01)
- Score sort works within tiers (FIX-02)
- Empty state shows helpful message (FIX-03)
- Search is trimmed and debounced (FIX-04)
- Negative pages guarded (FIX-05)
- Deal stacking reflows on mobile (FIX-06)
- Sign-in text fits container (FIX-07)
- No regressions in existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/04-foundation/04-04-SUMMARY.md`
</output>
