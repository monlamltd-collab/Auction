---
phase: 08-field-extraction-validation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - VAL-01

must_haves:
  truths:
    - "Lot cards never show 'Beds: ?' chip — if beds is null the chip is simply absent"
    - "Lot cards never show 'Tenure: ?' chip — if tenure is null the chip is simply absent"
    - "Lot cards never show a propType chip with null/empty value"
    - "Lots with no beds/tenure/propType still display fully — price, address, link all present"
    - "Lots WITH beds/tenure/propType still show those chips correctly (no regression)"
  artifacts:
    - path: "index.html"
      provides: "card() function with silent-omit for supplementary fields (beds, tenure, propType)"
      contains: "detailPills"
  key_links:
    - from: "index.html:card() function"
      to: "detailPills array"
      via: "remove else-branch push for beds gap chip and tenure gap chip"
      pattern: "Beds: \\?"
---

<objective>
Remove the "?" gap chips for beds, tenure, and propType from the lot card display in index.html. If a supplementary field is null, its chip simply does not appear — the lot remains fully visible with all structural fields (price, address, link) intact.

Purpose: VAL-01 (per D-03) — "?" chips are worse than nothing; they make the product look broken rather than simply incomplete. Silent omission is the correct UX for missing supplementary data.

Output: Targeted removal of 2-4 else branches in the card() function in index.html. No new logic added — only removal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-field-extraction-validation/08-RESEARCH.md

<interfaces>
<!-- Key patterns to change. Read these exact lines before editing. -->

From index.html card() function (~line 3463) — CURRENT code to remove:

Beds gap chip (remove the else branch entirely):
```javascript
// KEEP:
if (l.beds != null) detailPills.push({text: l.beds + ' bed', ...});
// REMOVE THIS ELSE:
else detailPills.push({text: 'Beds: ?', tip: '...', gap: true});
```

Tenure gap chip (remove the else branch entirely):
```javascript
// KEEP:
if (l.tenure) {
  detailPills.push({text: tenureLabel, ...});
// REMOVE THIS ELSE:
} else {
  detailPills.push({text: 'Tenure: ?', tip: '...', gap: true});
}
```

PropType gap chip (remove only the null/empty case if one exists):
```javascript
// Only push propType chip if l.propType is a non-empty, non-null value
// If there is an else branch pushing a '?' variant, remove it
```

Also check the expanded panel (~line 4241) for the same "?" patterns — if present, apply the same removal.

Field classification (per D-04 — do NOT change these):
  Always shown (structural): price, address, url/link, lot number
  Omit if missing (supplementary): beds, tenure, propType, sqft, leaseLength
  sqft is already omitted silently — do not change its handling
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove gap chips for beds, tenure, and propType in card() and expanded panel</name>
  <files>index.html</files>
  <read_first>
    - index.html lines 3440-3520 (card() function — find all detailPills.push calls for beds, tenure, propType including the gap/? variants)
    - index.html lines 4220-4280 (expanded panel — check for matching gap chip patterns for beds, tenure, propType)
  </read_first>
  <action>
    Search index.html for every occurrence of the following patterns and remove the else branches that push gap chips:
      1. "Beds: ?" — find the else branch that pushes this text to detailPills and delete the entire else block
      2. "Tenure: ?" — find the else branch that pushes this text to detailPills and delete the entire else block
      3. Any chip push for propType where the value is null, empty, or the literal string '?' — delete that branch

    For each removal:
      - Keep the if-branch that pushes the chip when the field IS present (no change to positive case)
      - Delete only the else block (the curly braces and their entire content)
      - Do not leave dangling else-if chains — if the pattern is if/else with no else-if, simply remove the else block and its braces

    Check the expanded panel (~line 4241) for the same patterns. If identical gap chips exist there, apply the same removal.

    Also check for any 'gap: true' flag on chips — after removal, there should be no chips with gap:true (or if the flag exists on legitimate chips for a different purpose, do not touch those). Search: `gap: true` in index.html to audit.

    Do NOT touch:
      - Any chips for price, address, lot number, link/url (structural fields always shown)
      - The positive if-branch for beds, tenure, propType (when the field has a value)
      - sqft handling (already omitted silently — leave as-is)
      - leaseLength (shown inline with tenure when present — leave as-is)
      - Any other display logic unrelated to these three fields

    After changes, the card() function should have:
      if (l.beds != null) detailPills.push(...beds chip...);
      // no else
      if (l.tenure) detailPills.push(...tenure chip...);
      // no else
      if (l.propType) detailPills.push(...propType chip...);
      // no else (or no propType chip at all if there was no positive case)
  </action>
  <verify>
    <automated>grep -n "Beds: ?\|Tenure: ?\|gap: true" index.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Beds: ?" index.html` returns 0 (the literal text "Beds: ?" is gone)
    - `grep -c "Tenure: ?" index.html` returns 0 (the literal text "Tenure: ?" is gone)
    - `grep -c "gap: true" index.html` returns 0 (no remaining gap chips)
    - `grep -c "l\.beds != null\|l\.beds)" index.html` returns at least 1 (positive beds chip preserved)
    - `grep -c "l\.tenure" index.html` returns at least 1 (positive tenure chip preserved)
  </acceptance_criteria>
  <done>All "?" gap chips removed from card() and expanded panel; positive chip branches untouched; structural fields (price, address, link) unaffected</done>
</task>

</tasks>

<verification>
grep audit after task:
  grep -n "Beds: ?\|Tenure: ?\|gap: true" index.html
  Expected: no matches

Positive case preserved:
  grep -n "l\.beds != null\|detailPills.*bed\b" index.html
  Expected: at least 1 match (the chip for when beds IS present)

Visual verification (deferred to phase checkpoint in plan 09): open the directory, find a lot with no bedroom count, confirm no "?" chip appears. Find a lot WITH a bedroom count, confirm the beds chip still shows.
</verification>

<success_criteria>
1. `grep "Beds: ?" index.html` returns no matches
2. `grep "Tenure: ?" index.html` returns no matches
3. `grep "gap: true" index.html` returns no matches
4. `grep "l.beds != null\|l\.beds)" index.html` returns at least 1 match (beds chip preserved for when beds has a value)
</success_criteria>

<output>
After completion, create `.planning/phases/08-field-extraction-validation/08-03-SUMMARY.md`
</output>
