---
phase: 08-field-extraction-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - server.js
  - admin.html
autonomous: true
requirements:
  - VAL-03

must_haves:
  truths:
    - "GET /api/quality-report returns fieldCoverage per house (beds%, tenure%, price%, images%, propType%)"
    - "Admin Operations tab shows a Field Coverage Per House table below the Cached Houses section"
    - "Cells below 50% are red, cells below 70% are amber, cells 70%+ are unstyled"
    - "Table renders inside loadQualityReport() with no separate fetch and no data race"
  artifacts:
    - path: "server.js"
      provides: "/api/quality-report response includes fieldCoverage per entry (calls validateBatch internally)"
      contains: "fieldCoverage"
    - path: "admin.html"
      provides: "Field coverage section with id=field-coverage-table rendered by loadQualityReport()"
      contains: "field-coverage-tbody"
  key_links:
    - from: "server.js:/api/quality-report"
      to: "validateBatch(lots, house)"
      via: "call validateBatch after dedup step, include fieldCoverage in entry object"
      pattern: "validateBatch.*lots.*house"
    - from: "admin.html:loadQualityReport()"
      to: "#field-coverage-tbody"
      via: "data.houses.forEach — render rows inside the existing fetch .then() block"
      pattern: "field-coverage-tbody"
---

<objective>
Surface the per-house field coverage data that validateBatch() already computes but never returns. Extend /api/quality-report to include fieldCoverage in each house entry, then render it as a colour-coded table in the admin Operations tab.

Purpose: VAL-03 — admin needs to see at a glance which auction houses have coverage gaps for beds, tenure, price, images, and propType so problems are diagnosed before they affect users.

Output: API change (2 lines in server.js) + HTML/JS addition in admin.html (a new details section + table rendering in loadQualityReport).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-field-extraction-validation/08-01-SUMMARY.md
@.planning/phases/08-field-extraction-validation/08-RESEARCH.md

<interfaces>
<!-- Key contracts the executor needs. Read before touching any file. -->

From server.js /api/quality-report handler (~line 5702):
The handler builds one `entry` object per house. The current entry shape is:
  { house, lots: lots.length, rawLots: rawLots.length, images: withImage, imgCoverage, dupes, ageHours, stale }
After plan 08-01, validateBatch() returns { fieldCoverage: { imageUrl, price, address, tenure, beds, url, propType } }
where each value is a percentage integer 0-100.
Add fieldCoverage to the entry by calling validateBatch(lots, house) after the dedup step.

From admin.html loadQualityReport (~line 1269):
Currently fetches /api/quality-report and iterates data.houses to update image coverage cells.
After the existing table population loop, render rows into #field-coverage-tbody.
data.houses[i].fieldCoverage shape: { imageUrl, price, address, tenure, beds, url, propType }

Colour coding rule (inline style on td):
  pct >= 70: no background (normal)
  pct >= 50 and pct < 70: background: var(--text-amber) or '#e67e22' with opacity
  pct < 50: background: var(--accent-danger) or '#c0392b' with opacity

Existing admin.html details/summary pattern:
  Use native details/summary elements with padding:14px 18px on summary — matches existing collapsible sections.
  Do NOT use JS-driven show/hide toggles.

New section HTML (inject after Cached Houses section, before any issues/alerts section):
  id="field-coverage-section" on the wrapper div
  id="field-coverage-table" on the table
  id="field-coverage-tbody" on the tbody
  id="field-coverage-badge" on the count badge in summary
  colspan="6" on the loading row (6 columns: House + 5 field columns)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend /api/quality-report to return fieldCoverage per house</name>
  <files>server.js</files>
  <read_first>
    - server.js lines 5702-5800 (full /api/quality-report handler — find the dedup step, entry construction, and existing imports)
    - lib/harness/data-contract.js lines 1-20 (confirm validateBatch export name)
  </read_first>
  <action>
    In the /api/quality-report handler in server.js (~line 5702):

    Step 1 — Confirm import: Search the top of server.js for existing imports from lib/harness/data-contract.js. If validateBatch is not already imported, add it to the import. Use the same import statement pattern that exists for other harness functions (likely a named import: `import { validateBatch, ... } from './lib/harness/data-contract.js';`).

    Step 2 — Call validateBatch: After the lot deduplication step (the step that produces the final `lots` array for each house), add:
      const { fieldCoverage } = validateBatch(lots, house);
    Wrap in try/catch with empty catch if the handler does not already have broad error handling around this block, so a malformed lot array cannot break the entire report.

    Step 3 — Add to entry: Add fieldCoverage as a field on the entry object alongside the existing fields. Add a trailing comma after the last existing field before adding fieldCoverage (per project trailing-comma convention).

    Do NOT modify any other part of the quality-report handler. Do NOT change the response shape for any existing fields.
    Use 2-space indentation, single quotes throughout.
  </action>
  <verify>
    <automated>grep -n "fieldCoverage" server.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fieldCoverage" server.js` returns at least 2 (the validateBatch call and the entry field)
    - `grep -n "validateBatch" server.js` returns at least 1 match inside the quality-report handler
    - `grep -n "fieldCoverage" server.js | grep "entry\|const"` returns matches for both the destructure and the entry assignment
    - The handler region (lines 5702-5800 approx) has no syntax errors — verify by running `node --check server.js`
  </acceptance_criteria>
  <done>validateBatch called inside /api/quality-report; fieldCoverage included in each house entry; node --check server.js passes</done>
</task>

<task type="auto">
  <name>Task 2: Add Field Coverage table to admin Operations tab</name>
  <files>admin.html</files>
  <read_first>
    - admin.html lines 340-500 (Operations tab — find the Cached Houses section end and identify the exact insertion point for the new section)
    - admin.html lines 1260-1350 (loadQualityReport function — understand data flow and where to add tbody population)
    - admin.html lines 1-50 (find CSS variable definitions — confirm var(--red), var(--amber) or equivalent names)
  </read_first>
  <action>
    Step 1 — Add HTML section:
    In the Operations tab HTML, after the closing tag of the Cached Houses table section and before any issues/alerts section, insert the following HTML block (adapt whitespace/indentation to match surrounding code):

    ```html
    <div class="section" id="field-coverage-section">
      <details>
        <summary style="padding:14px 18px;font-size:.88rem;font-weight:700;cursor:pointer;">
          Field Coverage Per House
          <span class="badge" id="field-coverage-badge" style="font-size:.7rem;">-</span>
        </summary>
        <div style="overflow-x:auto;">
          <table id="field-coverage-table">
            <thead>
              <tr>
                <th>House</th>
                <th title="% of lots with bedroom count">Beds %</th>
                <th title="% of lots with freehold/leasehold">Tenure %</th>
                <th title="% of lots with guide price">Price %</th>
                <th title="% of lots with image URL">Images %</th>
                <th title="% of lots with normalised property type">PropType %</th>
              </tr>
            </thead>
            <tbody id="field-coverage-tbody">
              <tr><td colspan="6" class="loading"><span class="spinner"></span> Loading...</td></tr>
            </tbody>
          </table>
        </div>
      </details>
    </div>
    ```

    Step 2 — Add rendering logic in loadQualityReport():
    Inside loadQualityReport(), after the existing loop that populates the Cached Houses table rows (the loop over data.houses), add code to populate #field-coverage-tbody. Follow this logic exactly:

    a. Build a helper function (inline or extracted) to map a percentage to a background style string:
       - pct < 50: 'background:#c0392b;color:#fff;'
       - pct >= 50 and pct < 70: 'background:#e67e22;color:#fff;'
       - pct >= 70: ''  (no style — normal cell)
       Use the literal hex values above rather than CSS variables to avoid any var() resolution issues in the table cells.

    b. Sort data.houses by house name (or use the existing order — do not change the existing sort if one is in place).

    c. Build a tbody innerHTML string by iterating data.houses. For each house h:
       - Skip houses where h.fieldCoverage is null or undefined (graceful degradation if server is on an old version)
       - columns: h.house, h.fieldCoverage.beds, h.fieldCoverage.tenure, h.fieldCoverage.price, h.fieldCoverage.imageUrl, h.fieldCoverage.propType
       - Apply the colour helper to each numeric column
       - Display format: integer percentage with '%' suffix, e.g. '73%'
       - If a column value is null/undefined, display '-'

    d. Set document.getElementById('field-coverage-tbody').innerHTML = rows.join('');

    e. Update the badge: document.getElementById('field-coverage-badge').textContent = data.houses.length + ' houses';

    f. Render inside the existing .then() or await block of the apiFetch('/api/quality-report') call — do NOT create a separate fetch call or event listener. This prevents the data race described in research Pitfall 5.

    Use 2-space indentation, single quotes throughout. Follow the existing admin.html JS style (likely uses template literals for HTML building).
  </action>
  <verify>
    <automated>grep -n "field-coverage-tbody\|field-coverage-table\|field-coverage-section" admin.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "field-coverage-tbody" admin.html` returns at least 2 (HTML definition + JS population)
    - `grep -c "field-coverage-table" admin.html` returns at least 1
    - `grep -c "fieldCoverage" admin.html` returns at least 3 (accessing .beds, .tenure, .price at minimum)
    - `grep -n "c0392b\|e67e22" admin.html` returns matches in the field coverage render function (colour coding present)
    - `grep "field-coverage-section" admin.html` returns a match inside the Operations tab HTML
    - Opening admin.html in browser and navigating to Operations tab shows "Field Coverage Per House" collapsible section (checkpoint:human-verify handled in plan 09 visual verification)
  </acceptance_criteria>
  <done>Field coverage section HTML present in Operations tab; loadQualityReport() populates tbody with colour-coded rows; no separate fetch introduced</done>
</task>

</tasks>

<verification>
Syntax check after both tasks: `node --check server.js`

Manual API verification (requires running server):
  curl -s -H "x-admin-secret: $ADMIN_SECRET" http://localhost:$PORT/api/quality-report | node -e "const d=require('fs').readFileSync(0,'utf8'); const r=JSON.parse(d); const h=r.houses[0]; console.log('fieldCoverage:', h.fieldCoverage ? JSON.stringify(h.fieldCoverage) : 'MISSING');"
  Expected: fieldCoverage object with beds, tenure, price, imageUrl, propType keys each showing a percentage

Visual verification (open admin, Operations tab, scroll to Field Coverage section) is deferred to the phase checkpoint in plan 09.
</verification>

<success_criteria>
1. `grep -c "fieldCoverage" server.js` returns at least 2
2. `grep -c "field-coverage-tbody" admin.html` returns at least 2
3. `node --check server.js` exits 0
4. `grep "c0392b\|e67e22" admin.html` confirms colour thresholds are coded
</success_criteria>

<output>
After completion, create `.planning/phases/08-field-extraction-validation/08-02-SUMMARY.md`
</output>
